{
  "name": "kuvaira",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "npm run build && vite preview --host 0.0.0.0 --port 8080",
    "build": "node scripts/build-static.mjs",
    "preview": "vite preview --host 0.0.0.0 --port 8080"
  },
  "devDependencies": {
    "vite": "^5.4.14"
  }
}
